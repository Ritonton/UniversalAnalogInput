<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UniversalAnalogInputUI.Views.StatusMonitorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="15,0,15,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status -->
        <TextBlock Grid.Row="0" Text="System Status" 
                   Style="{StaticResource SubtitleTextBlockStyle}" 
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Border x:Name="StatusBorder" 
                    Background="{ThemeResource SurfaceCardBackgroundBrush}" 
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    Padding="12,8" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon x:Name="StatusIcon" 
                             FontFamily="Segoe MDL2 Assets" 
                             Glyph="&#xE783;"
                             FontSize="14" 
                             Foreground="#FFDC2626"/>
                    <TextBlock x:Name="StatusText"
                              Text="System not initialized"
                              FontWeight="SemiBold"
                              Foreground="Black"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <TextBlock x:Name="MappingCountText" 
                      Text="0 mappings configured"
                      Style="{StaticResource CaptionTextBlockStyle}" 
                      Foreground="{ThemeResource SubtleForegroundBrush}"/>
        </StackPanel>

        <!-- Live Input Display -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                      Text="Live Input"
                      Style="{StaticResource SubtitleTextBlockStyle}"
                      Margin="0,0,0,10"/>

            <Border Grid.Row="1"
                    Background="{ThemeResource SurfaceCardBackgroundBrush}"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    Padding="15">
                <ScrollViewer x:Name="LiveInputScrollViewer"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="LiveInputText"
                              Text="Loading..."
                              Foreground="{ThemeResource SubtleForegroundBrush}"
                              FontFamily="Consolas"
                              FontSize="11"
                              TextWrapping="Wrap"
                              IsTextSelectionEnabled="True"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
